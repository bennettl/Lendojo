<%  # Remote: true uses ajax 
	# Find an existing user service 
	user_service = UserService.find(user_service_id)
%>
<%=	form_for user_service, remote: true, html: {method: :delete, id: 'unpin_form' }, url: { controller:'user_services', action: 'destroy_pin', id: user_service_id } do |f|  %>
	<%= link_to('', '#', class: "option tooltip_target glyphicon glyphicon-minus pin",  data: {toggle: 'tooltip'}, title: "Unpin")  %>
<% end %>