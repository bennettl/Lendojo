<%= link_to('', check_service_path(service, user_id: current_user.id ), 
				method: :post,
				remote: true,
				class: "option check tooltip_target glyphicon glyphicon-ok", 
				data: { toggle: 'tooltip' }, 
				title: 'Add To Checklist')  %>
