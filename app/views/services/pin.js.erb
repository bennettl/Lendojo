// When a user service is created, executed the following js: render the _unpin partial
jQuery('.options .option.pin').replaceWith("<%= escape_javascript( render('shared/options/unpin', service: @service)) %>");
