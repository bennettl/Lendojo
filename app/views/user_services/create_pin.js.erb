// When a user service is created, executed the following js: render the _unpin partial
jQuery('#pin_form').replaceWith("<%= escape_javascript(render('shared/options/unpin', user_service_id: @user_service.id) ) %>");
